# pnpm settings (pnpm 11 reads project config from here, not the package.json "pnpm" field).

# Patched floors for vulnerable transitive dev dependencies. Every one of these reaches
# the tree through the same chain, wxt > web-ext-run > ..., which is the firefox runner:
# nothing here ships in the extension, and `pnpm audit --prod` is clean without any of it.
#
# These are floors (`>=`), not pins, so each install takes the newest release rather than
# the one that happened to be current when the advisory was read. That is deliberate: the
# pinned form of this block went stale three rounds running, every time a later advisory
# raised the floor on a package that was already listed. A floor cannot go stale that way.
# The version named in each comment is the one the advisory requires, not a ceiling.
#
# What is NOT here matters as much. js-yaml, postcss, nanoid and both brace-expansion
# lines used to be pinned here and are gone: they were transitive dependencies of an
# older eslint and vitest, and on eslint 10 / vitest 5 the tree resolves them patched on
# its own. Re-run `pnpm audit` with this block emptied before adding a pin, rather than
# inheriting one.
#
# esbuild is deliberately absent, and re-tested rather than assumed: with `esbuild:
# ^0.28.1` the install succeeds and `pnpm build` then fails with 160 "Transforming
# destructuring to the configured target environment is not supported yet" errors against
# the manifest's browser targets. esbuild is a direct dependency of wxt, so no amount of
# deduplicating vite reaches it. Its one advisory (GHSA-g7r4-m6w7-qqqr) is a low-severity
# dev-server-on-Windows file read, below the `high` audit gate and not exercised by the
# build.
overrides:
  shell-quote: '>=1.8.5' # GHSA-g4vp-m682-qqmp (critical, newline escaping) + a quadratic DoS
  tmp: '>=0.2.6' # path traversal via an unsanitized prefix
  uuid: '>=11.1.1' # missing buffer bounds check in v3/v5/v6
  adm-zip: '>=0.6.1' # GHSA-7q85-xj36-vmfc (memory allocation) + GHSA-vwc7-r8mq-g2x9 (symlinks)

# lzutf8 (transitive, via @marksyncorg/core) writes its optional debug output with
# `document.body.innerHTML += …`. The branch is dead here — nothing ever asks it to log
# to the document — but the bundler cannot prove that, so the string ships in
# background.js and in the popup/options chunk and the AMO validator flags it as an
# unsafe innerHTML assignment. The patch keeps the same behaviour using text nodes.
# Pinned to the exact version: a lzutf8 bump fails the install until the patch is
# re-checked, rather than silently dropping it.
patchedDependencies:
  lzutf8@0.6.3: patches/lzutf8@0.6.3.patch

# HALF-CLOSED GAP. The lockfile used to carry no `patchedDependencies` section, so it
# disagreed with the block above and `pnpm install --frozen-lockfile` refused outright
# with ERR_PNPM_LOCKFILE_CONFIG_MISMATCH. That half is done: the lockfile was regenerated
# with a GitHub Packages token, it records the patch, and a frozen install now succeeds
# from a clean node_modules.
#
# The other half is not. All seven workflow installs still pass `--no-frozen-lockfile`,
# so CI, nightly and release alike re-resolve the tree from the registry instead of
# building what was reviewed, and the release job is the one that builds what ships to
# the stores. Moving them back to `--frozen-lockfile` is now a safe edit, with one
# ordering constraint: it should land after Dependabot can actually update this lockfile,
# which needs the DEPENDABOT_PACKAGES_TOKEN secret named in .github/dependabot.yml.
# Before then, a Dependabot pull request that touches package.json without the lockfile
# would fail CI on the mismatch rather than on anything real.

# pnpm blocks dependency lifecycle scripts by default. esbuild needs its install script
# to fetch the platform binary used by vite/wxt; nothing else needs to build.
# spawn-sync is listed as an explicit `false` rather than left out: it is a transitive of
# the firefox runner, and saying no in the file is a decision on the record instead of an
# omission someone has to re-derive.
allowBuilds:
  esbuild: true
  spawn-sync: false

# Supply-chain cooldown: never install a package version published less than 4 days ago
# (4 * 24 * 60 = 5760 minutes), giving freshly-published (possibly compromised) releases
# time to be caught before they enter the tree. It gates new resolutions, which is what
# every CI, nightly and release install still performs while those installs pass
# `--no-frozen-lockfile` (see the gap note above). So the cooldown remains, for now, the
# only thing standing between a fresh upstream release and a build that ships.
#
# It also holds back versions this repo would otherwise take: today eslint 10.11.0 and
# prettier 3.9.8 are both inside the window, which is why the manifest names 10.10.0 and
# 3.9.6. That is the cooldown working, not a stale pin.
minimumReleaseAge: 5760

# Exempt our own first-party package from the cooldown: it is published by us to a
# private registry (not a public supply-chain risk), so new releases must install
# immediately without waiting out the window.
minimumReleaseAgeExclude:
  - '@marksyncorg/core'
